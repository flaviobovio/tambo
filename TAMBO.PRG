PARAMETER DIRECC

direcc = iif( direcc#nil, upper(direcc), nil )

*
RUN ("CD ESTRUC")
public tambos:=di_tamb()
*
SET MESSAGE TO 24 CENTER
SET CURSOR OFF
SET TALK OFF
SET SCOREBOARD OFF
SET STATUS OFF
SET DATE FRENCH
SET CONFIRM ON
SET ESCAPE OFF
SET WRAP ON
SET DELETE ON
SET DECIMALS TO 0
SET EPOCH TO 1975
SETCANCEL(.F.)
READEXIT(.F.)
SET KEY 17 TO MENU
SET KEY 28 TO AYUDA
*

IF ASCAN( tambos, DIRECC ) # 0

   RUN ("CD..")
   RUN ("CD "+DIRECC)

ENDIF

*DEMO
*PUBLIC TAMBO:=CHR(68)+CHR(69)+CHR(77)+CHR(79)

*FAGE
*PUBLIC TAMBO:=CHR(68)+CHR(114)+CHR(46)+CHR(77)+CHR(46)+;
*              CHR(70)+CHR(65)+CHR(71)+CHR(69)

*Cledou
*PUBLIC TAMBO:=CHR(70)+CHR(46)+CHR(67)+CHR(76)+CHR(69)+;
*              CHR(68)+CHR(79)+CHR(85)

* Vaca Pampa
*PUBLIC TAMBO:=CHR( 86)+CHR( 65)+CHR( 67)+CHR( 65)+CHR( 32)+;
*               CHR( 80)+CHR( 65)+CHR( 77)+CHR( 80)+CHR( 65)

* Los Robles
*PUBLIC TAMBO:=CHR( 76)+CHR( 79)+CHR( 83)+CHR( 32)+CHR( 82)+;
*               CHR( 79)+CHR( 66)+CHR( 76)+CHR( 69)+CHR( 83)

*Irribarren
PUBLIC TAMBO:=CHR( 73)+CHR(114)+CHR(105)+CHR( 98)+CHR( 97)+;
               CHR(114)+CHR(114)+CHR(101)+CHR(110)





USE CONFIG.CFG                  NEW
PUBLIC DOSRIGID:=RIGIDO, DOSDRIVE:=FLEXIBLE , DOSPATH:= PATHDOS
PUBLIC COMUN, INVER ,TITIL, NEGRO

IF CONFIG->COLOR
   COMUN = "BG+/B, W+/N,,,BG+/N"
   INVER = "W+/R, BG+/B,,,W+/R"
   TITIL = "W+/R*, BG+/B*,,,W+/R*"
   NEGRO = "W+/N"
   GETS  = "BG+/N"
ELSE
   COMUN = "W/N,N/W+,,,N/W"
   INVER = "N/W,W+/N,,,N/W"
   TITIL = "I*"
   NEGRO = "W+/N"
   GETS  = "N/W"
ENDIF
*

USE TAMBO  INDEX TAMBO , PRODUC NEW
USE PARTOS INDEX PARTOS         NEW
USE ENFERM INDEX ENFERM         NEW
USE BACENF INDEX BACENF         NEW
USE SERVIC INDEX SERVIC         NEW
USE BACSER INDEX BACSER         NEW
USE TEMPO  INDEX TEMPO          NEW
USE PRODUC INDEX PROCOD, PROLIT NEW
D_GUA = (ALLTRIM(CONFIG->GUACHE))
USE (D_GUA+"GUACHE") INDEX (D_GUA+"GUACHE") NEW
USE (D_GUA+"ENFGUA") INDEX (D_GUA+"ENFGUA") NEW
USE CELULA INDE CELULA          NEW
use p_anual                     new
USE MOTIVO                      NEW
USE TRATAM                      NEW
USE TOROS                       NEW
USE INSEMI                      NEW
USE TIPOS                       NEW
USE VACTOT                      NEW
USE RODEOS                      NEW
USE AYUDA.HLP                   NEW
USE IMPRES.IMP                  NEW
PRESEN()
FEC_ACT()
MENU()
# INCLUDE 'PRESEN.PRG'
# INCLUDE 'FEC_ACT.PRG'
# INCLUDE 'ACT_DIA.PRG'
# INCLUDE 'MENU.PRG'
# INCLUDE 'PRINTOK.PRG'
# INCLUDE 'IMPRI.PRG'
# INCLUDE 'NUME.PRG'
# INCLUDE 'NUMENT.PRG'
# INCLUDE 'ACEPTA.PRG'
# INCLUDE 'MENSAJE.PRG'
# INCLUDE 'IMPRES.PRG'
# INCLUDE 'MES.PRG'
# INCLUDE 'DATES.PRG'
# INCLUDE 'MARCO.PRG'
# INCLUDE 'ACH_FUN.PRG'
# INCLUDE 'MOSTRAR.PRG'
# INCLUDE 'VACA.PRG'
# INCLUDE 'AYUDA.PRG'
# include 'di_tamb.prg'
# INCLUDE 'TERNER.PRG'
# INCLUDE 'EXI_TER.PRG'
# INCLUDE 'TID_TER.PRG'
